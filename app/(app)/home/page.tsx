import { ComingSoon } from '@/components/shell/coming-soon'

export default function HousePage() {
  return (
    <ComingSoon
      title="House"
      description="Share the load — chores, a cleaning schedule, maintenance reminders, home projects and shared lists, with a fairness view so it stays even."
    />
  )
}
