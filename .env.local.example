# Local Supabase (populated by `npx supabase start` output once Docker is installed)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<paste anon key from `npx supabase start` output>
SUPABASE_SERVICE_ROLE_KEY=<paste service_role key from `npx supabase start` output>
